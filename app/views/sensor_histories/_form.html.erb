<%= form_with(model: sensor_history, local: true) do |form| %>
  <% if sensor_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sensor_history.errors.count, "error") %> prohibited this sensor_history from being saved:</h2>

      <ul>
        <% sensor_history.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sensor_id %>
    <%= form.text_field :sensor_id %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
